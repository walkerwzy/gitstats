### Usage

```bash
git clone https://github.com/hoxu/gitstats.git
cd gitstats && sudo make install
sudo apt-get install python2 gnuplot
git clone <URL_REPOSITORY_TO_ANALIZE>
gitstats <REPOSITORY_DIR> <REPORT_FOLDER>
```
